# 클로드 코드(Claude Code) 사용법

Anthropic이 만든 AI 코딩 어시스턴트 CLI인 **Claude Code**의 핵심 사용법을 정리한 문서입니다.

## 1. 개요 및 설치

Claude Code는 터미널에서 실행되며 코드베이스 전체를 읽고, 파일을 수정하고, 명령을 실행하고, 여러 도구를 넘나들며 기능 구현·버그 수정·자동화 작업을 수행하는 AI 어시스턴트입니다. 터미널, IDE, 데스크톱 앱, 브라우저와 통합됩니다.

**설치 방법**

```bash
# macOS / Linux / WSL
curl -fsSL https://claude.ai/install.sh | bash
```

```bash
# Windows PowerShell
irm https://claude.ai/install.ps1 | iex
```

- Homebrew: `brew install --cask claude-code`
- WinGet: `winget install Anthropic.ClaudeCode`
- 설치 확인: `claude --version`

**필요 조건**: Claude Pro/Max/Team/Enterprise 구독, Claude Console 계정, 또는 AWS Bedrock·Google Cloud·Microsoft Foundry 같은 서드파티 제공자 계정 중 하나. 구독 계정이면 별도 API 키가 필요 없습니다.

## 2. 기본 사용법 (세션)

```bash
claude                     # 대화형 세션 시작
claude "작업 설명"          # 초기 프롬프트와 함께 시작
claude -p "질문"           # 프린트 모드: 한 번 응답하고 즉시 종료 (비대화형)
claude --bare -p "질문"    # 훅/스킬/자동 메모리/CLAUDE.md를 건너뛰는 빠른 모드 (CI 용)
```

**세션 이어가기**

- `claude --continue` (`-c`) — 현재 디렉터리의 가장 최근 대화 재개
- `claude --resume` (`-r`) — 세션 선택 화면 열기
- `claude --resume <세션ID|이름>` — 특정 세션 재개
- `claude -n <이름>` — 세션 시작 시 이름 지정

**세션 내부 명령**

- `/resume` — 다른 대화로 전환
- `/branch <이름>` — 지금까지의 대화를 복사해 새 브랜치 생성(원본 유지)
- `/clear` — 컨텍스트를 비우고 새로 시작 (이전 대화는 저장됨)
- `/exit` 또는 `Ctrl+D` 두 번 — 종료

세션은 `~/.claude/projects/<프로젝트>/<세션ID>.jsonl`에 자동 저장되어 나중에 어떤 디렉터리에서든 재개할 수 있습니다.

## 3. 자주 쓰는 슬래시 명령어

세션에서 `/`를 입력하면 전체 목록이 보입니다. 주요 명령어:

**세션·컨텍스트**
| 명령 | 설명 |
|---|---|
| `/help` | 사용 가능한 명령/스킬 보기 |
| `/clear` | 새 대화 시작 (이전 대화는 저장) |
| `/compact [지시]` | 대화 기록을 요약해 컨텍스트 확보 |
| `/context` | 컨텍스트를 차지하는 항목(메모리, 읽은 파일 등) 확인 |
| `/export` | 대화 내용을 텍스트로 내보내기 |

**설정·권한**
| 명령 | 설명 |
|---|---|
| `/config` | 설정 열기/변경 (`/config key=value`) |
| `/permissions` | 권한 규칙 관리 |
| `/mcp` | MCP 서버 연결 상태 관리 |
| `/memory` | CLAUDE.md 편집, 자동 메모리 관리/on-off |

**모델·성능**
| 명령 | 설명 |
|---|---|
| `/model` | 모델 전환 (opus, sonnet, haiku 등) |
| `/effort` | 추론 강도 설정 (low/medium/high/xhigh/max) |
| `/fast` | 패스트 모드 토글 |

**코드 품질**
| 명령 | 설명 |
|---|---|
| `/code-review` | 변경분(diff)의 버그·정리 대상 리뷰 |
| `/security-review` | 보안 관점 리뷰 |
| `/plan` | 플랜 모드 진입 (탐색 후 실행) |

**기타 유용한 명령**
| 명령 | 설명 |
|---|---|
| `/init` | 코드베이스 분석 후 CLAUDE.md 초안 생성 |
| `/add-dir <경로>` | 추가 작업 디렉터리 등록 |
| `/agents` | 서브에이전트 파일 위치 안내 |
| `/skills` | 사용 가능한 스킬 탐색 |
| `/plugin` | 플러그인 관리 |
| `/hooks` | 설정된 훅 확인 |
| `/doctor` | 설정 상태 점검 (중복, 미사용 플러그인, 느린 훅 등) |
| `/keybindings` | 키바인딩 설정 |
| `/cost` | 사용 비용 확인 |
| `/bug` | 버그 리포트 |
| `/vim` | vim 키바인딩 모드 |

## 4. CLAUDE.md와 메모리

**CLAUDE.md**는 Claude에게 지속적인 지침을 전달하는 마크다운 파일로, 세션 시작 시 항상 로드됩니다.

**위치 및 우선순위**
1. 조직 전체 관리 정책 (IT/DevOps가 배포)
2. `~/.claude/CLAUDE.md` — 사용자 개인 설정 (모든 프로젝트에 적용)
3. `./CLAUDE.md` — 프로젝트 공용 (버전 관리에 포함)
4. `./CLAUDE.local.md` — 프로젝트별 개인 설정 (`.gitignore`에 추가 권장)
5. 하위 디렉터리의 `CLAUDE.md`, `.claude/rules/*.md` (파일 패턴에 매칭될 때만 로드)

**작성 팁**
- 200줄 이내로 유지 — 빌드 명령, 코드 스타일, 아키텍처 결정, 테스트 방법, 주의사항 위주로 작성
- 코드를 보면 유추 가능한 내용, 장황한 API 문서는 제외 (링크로 대체)
- `@경로` 문법으로 다른 파일을 불러올 수 있음

**자동 메모리(Auto Memory)**

Claude는 세션 간 학습 내용을 자동으로 저장합니다. 위치: `~/.claude/projects/<프로젝트>/memory/`
- `MEMORY.md` — 색인 파일 (매 세션 로드)
- 주제별 파일(`user_role.md`, `feedback_testing.md` 등) — 필요 시 로드
- 종류: `user`(사용자 특성), `feedback`(피드백/교정), `project`(진행 중 작업), `reference`(외부 자료 위치)

`/memory`로 켜고 끄거나 편집할 수 있고, 프로젝트 설정의 `autoMemoryEnabled: false`로 특정 프로젝트에서 끌 수 있습니다.

## 5. 권한(Permissions) 시스템

`Shift+Tab`으로 권한 모드를 순환 전환할 수 있습니다.

| 모드 | 동작 |
|---|---|
| **default (수동)** | 파일 읽기·읽기 전용 명령만 자동 허용, 나머지는 매번 승인 요청 |
| **auto** | 위험도를 분류해 안전한 작업은 자동 승인 (Pro/Max/Team 대화형 세션 기본값) |
| **acceptEdits** | 파일 쓰기 및 일반적인 파일 시스템 작업을 자동 허용 |
| **plan** | 읽기 전용, 파일 수정·명령 실행 불가 — 탐색·계획 전용 |
| **bypassPermissions** | 모든 작업 자동 허용 (신뢰된 환경에서만 사용) |

`.claude/settings.json`에서 규칙을 직접 지정할 수 있습니다:

```json
{
  "permissions": {
    "defaultMode": "auto",
    "allow": ["Read", "Bash(npm test *)", "Bash(git commit *)"],
    "deny": ["Write(/secrets/*)", "Bash(rm -rf *)"]
  }
}
```

## 6. 훅(Hooks)

훅은 특정 시점에 항상 실행되는 셸 명령입니다(CLAUDE.md와 달리 예외 없이 실행). `.claude/settings.json`의 `hooks` 항목에 정의합니다.

| 이벤트 | 실행 시점 | 활용 예 |
|---|---|---|
| `PreToolUse` | 도구 실행 직전 | 명령 검증, 특정 경로 쓰기 차단 |
| `PostToolUse` | 도구 실행 직후 | 파일 수정 후 자동 포맷/린트 |
| `SessionStart` / `SessionEnd` | 세션 시작/종료 | 환경 초기화, 트랜스크립트 아카이브 |
| `Stop` | 세션 종료 직전 | 최종 검증 실행, 통과 전 종료 차단 |

```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [{ "type": "command", "command": "eslint --fix {file_path}" }]
      }
    ]
  }
}
```

## 7. MCP(Model Context Protocol) 서버

MCP는 Claude를 외부 도구·데이터 소스(Jira, Notion, DB, Figma 등)와 연결하는 개방형 표준입니다.

```bash
# HTTP 서버 추가
claude mcp add --transport http github https://api.github.com/mcp

# 인증 헤더와 함께 추가
claude mcp add --transport http notion https://mcp.notion.com --header "Authorization: Bearer TOKEN"

# 로컬 stdio 서버 추가
claude mcp add --transport stdio db -- npx @bytebase/dbhub --dsn "postgresql://..."

# 관리
claude mcp list
claude mcp get <이름>
claude mcp remove <이름>
```

**범위(scope)**: `local`(현재 프로젝트, 개인 전용, 기본값) / `project`(`.mcp.json`으로 팀 공유) / `user`(모든 프로젝트, 개인 전용).

## 8. 서브에이전트(Subagents)

서브에이전트는 독립된 컨텍스트 창에서 특정 작업을 처리하는 전문화된 보조 에이전트입니다. 많은 파일을 탐색해야 하거나 특화된 작업이 필요할 때 메인 대화의 컨텍스트를 아끼기 위해 사용합니다.

**정의 위치**: `.claude/agents/<이름>.md` (프로젝트) 또는 `~/.claude/agents/<이름>.md` (사용자)

```yaml
---
name: code-reviewer
description: 코드 리뷰가 필요할 때 사용. PR 분석, 품질 점검에 적합.
tools: Read, Grep, Glob, Bash
model: opus
---
당신은 시니어 코드 리뷰어입니다. 구조, 에러 처리, 보안, 테스트 커버리지를 점검하세요.
```

- 자연어로 호출: "code-reviewer 에이전트로 인증 변경분을 확인해줘"
- 세션 전체 기본값: `claude --agent code-reviewer`
- 내장 서브에이전트: `Explore`(빠른 읽기 전용 탐색), `Plan`(플랜 모드 조사), `general-purpose`(범용)

## 9. 커스텀 슬래시 명령어와 스킬

**스킬(Skill)**은 재사용 가능한 작업 절차를 패키징한 것으로, `~/.claude/skills/<이름>/SKILL.md` 또는 `.claude/skills/<이름>/SKILL.md`에 정의합니다.

```yaml
---
description: 어떤 상황에 이 스킬을 쓸지 설명 (자동 호출 판단에 사용됨)
allowed-tools: [Bash, Read]
---
## 지침
1. 이슈 내용을 확인한다
2. 관련 파일을 찾는다
3. 수정 후 테스트한다
```

`/스킬이름`으로 수동 호출하거나, 설명이 상황과 맞으면 Claude가 자동으로 사용합니다.

**플러그인(Plugin)**은 스킬·에이전트·MCP 서버·훅을 묶어 배포하는 단위로, `.claude-plugin/plugin.json` 매니페스트를 가진 디렉터리입니다. `/plugin` 명령으로 설치·관리합니다.

## 10. IDE 통합

**VS Code**: 확장 탭에서 "Claude Code" 검색 후 설치. 인라인 diff, `@파일` 멘션, 플랜 리뷰 등을 지원.

**JetBrains(IntelliJ, PyCharm, WebStorm 등)**: CLI를 먼저 설치한 뒤 JetBrains Marketplace에서 플러그인 설치. 에디터에서 `Cmd+Esc`/`Ctrl+Esc`로 실행하거나 통합 터미널에서 `claude` 실행. 외부 터미널에서는 `/ide`로 연결.

## 11. 유용한 키보드 단축키

| 단축키 | 기능 |
|---|---|
| `Shift+Tab` | 권한 모드 순환 전환 |
| `Ctrl+C` | 현재 작업 중단 |
| `Ctrl+D` (두 번) | 종료 |
| `Ctrl+J` | 줄바꿈 삽입 (전송 안 함) |
| `Esc` | 입력/작업 취소 |
| `Esc` 두 번 | 되감기(rewind) 메뉴 열기 |
| `Ctrl+R` | 히스토리 검색 |
| `Ctrl+V` (WSL: `Alt+V`) | 클립보드 이미지 붙여넣기 |

키바인딩은 `/keybindings` 명령으로 `~/.claude/keybindings.json`을 열어 커스터마이즈할 수 있습니다.

## 12. 자주 쓰는 CLI 플래그

```bash
claude -p "질문"                        # 비대화형 실행
claude --model opus                     # 모델 지정
claude --permission-mode plan           # 시작 권한 모드 지정
claude --dangerously-skip-permissions   # 모든 권한 확인 생략 (주의해서 사용)
claude --add-dir ../other-project       # 추가 디렉터리 등록
claude --output-format json             # 출력 형식 (스크립트 연동용)
claude --mcp-config config.json         # MCP 설정 전달
claude --debug                          # 디버그 로그
```

## 13. 효과적으로 사용하는 팁

- **컨텍스트 관리**: 컨텍스트 창이 차면 성능이 떨어지므로, 관련 없는 작업 사이에는 `/clear`를 적극 사용하세요. 대화가 길어지면 `/compact`로 요약할 수 있습니다.
- **검증 수단을 제공하세요**: 그냥 구현을 요청하기보다 테스트·빌드 명령·스크린샷처럼 성공/실패를 판단할 수 있는 기준을 함께 주면 Claude가 스스로 검증하고 반복 개선할 수 있습니다.
- **먼저 탐색, 그다음 계획, 마지막에 구현**: 플랜 모드(`Shift+Tab`으로 진입)에서 충분히 조사한 뒤 계획을 세우고, 이후 구현으로 전환하면 엉뚱한 문제를 푸는 것을 방지할 수 있습니다.
- **구체적인 프롬프트가 낫습니다**: "버그 고쳐줘"보다는 "로그인 세션 타임아웃 후 실패한다. src/auth/ 특히 토큰 갱신 로직을 확인하고, 실패하는 테스트를 먼저 작성한 뒤 고쳐줘"처럼 구체적으로 요청하세요.
- **CLAUDE.md는 짧고 밀도 있게**: 200줄 이내, 코드에서 유추 가능한 내용은 빼고 구체적인 규칙 위주로 작성하면 버전 관리와 함께 그 가치가 누적됩니다.
- **일찍 방향을 바로잡으세요**: `Esc`로 중단하면 컨텍스트는 유지된 채 멈춥니다. 같은 문제로 두 번 이상 고쳐야 한다면 `/clear` 후 배운 내용을 반영해 프롬프트를 다시 작성하는 편이 낫습니다.
- **자동화/CI에서는**: `claude -p`로 스크립트에 넣고, `--allowedTools`로 안전한 도구만 미리 승인하거나 `--bare`로 훅/플러그인을 건너뛰어 실행 속도를 높이세요.

## 참고 자료

- 공식 문서: https://code.claude.com/docs/en/
- CLI 도움말: `claude --help`
- 문제 진단: `/doctor`
